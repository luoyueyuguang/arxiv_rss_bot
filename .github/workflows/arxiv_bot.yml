name: arXiv Bot Daily Update

on:
  push:
    branches: [ main, master ]
    paths:
      - 'config.json'
      - 'arxiv_bot.py'
      - 'CUSTOM_RULES.md'
      - 'example_custom_rules.py'
      - 'rss_service.py'
      - '.github/workflows/arxiv_bot.yml'
  schedule:
    - cron: '40 5 * * 1-5' # utc+8 14:40 on Monday-Friday
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  update-papers:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        fetch-depth: 0  # Fetch all history for proper git operations
      
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'
        
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        
    - name: Run arXiv Bot
      env:
        ARK_API_KEY: ${{ secrets.ARK_API_KEY }}
      run: |
        python arxiv_bot.py
        
    - name: Commit and push changes
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        git add README.md
        git diff --staged --quiet || git commit -m "🤖 Update papers from arXiv - $(date -u +'%Y-%m-%d %H:%M:%S UTC')"
        git pull --rebase origin master
        git push
        
    - name: Show updated papers count
      run: |
        echo "📊 Papers updated in README.md"
        grep -c "### [0-9]*\." README.md || echo "No papers found" 
